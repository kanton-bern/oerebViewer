'use strict';

/**
 * @ngdoc function
 * @name oerebApp.controller:AboutCtrl
 * @description
 * # AboutCtrl
 * Controller of the oerebApp
 */
angular.module('oerebApp')
    .controller('DetailCtrl', function () {
        this.info = [
            'detail'
        ];
    }
);
